name: 05-01-filters-activity-types

on:
    pull_request:
        types: [opened, synchronize, reopened]
        branches:
            - main

jobs:
    echo-activity-types:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Running whenever a PR is opened or synchronized against the main branch."
        