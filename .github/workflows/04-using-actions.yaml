name: 04-Using-Actions-python-runner

on: workflow_dispatch

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v6
          - name: Printing Folders
            run: ls -la


